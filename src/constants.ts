/** Auto-refresh interval for data polling (5 minutes) */
export const POLL_INTERVAL_MS = 5 * 60 * 1000;

/** Auto-refresh interval for live match polling (15 seconds) */
export const LIVE_POLL_INTERVAL_MS = 15_000;
